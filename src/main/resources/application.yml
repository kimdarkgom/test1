
spring:
  mustache:
    suffix: .html
    expose-session-attributes: true

  datasource:
    url: jdbc:mariadb://localhost:3307/gamedb
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 1234
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update